  image: alpine:latest

  pages:
    stage: deploy
    script:
      - echo "Deploying to GitLab Pages"
    artifacts:
      paths:
        - public
    rules:
      - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH